# Test fixture: a .env-named file. Content is intentionally benign — the
# safety check should reject it on path-pattern match alone, before ever
# reading content. Do NOT put real secrets here.
EXAMPLE_FLAG=true
NOT_A_REAL_SECRET=placeholder
